# Topology

- `physical-topology.png` — physical topology diagram (edge/ISP, core, access, end devices)
- `logical-topology.png` — logical topology diagram (VLANs, IP addressing, trunking)
- `Khumo_Physical_Topology.drawio` — editable draw.io source for the physical topology
- `Khumo_Logical_Topology.drawio` — editable draw.io source for the logical topology

See [`../03-ip-addressing/`](../03-ip-addressing) for the full VLAN table and IP addressing plan referenced in these diagrams.
