# Device Configuration

Configuration exports (running-config) for each device will be added here as the build progresses.

Planned contents:
- Core L3 switch — VLANs, SVIs, inter-VLAN routing
- Access switches — trunk and access port configuration
- Edge routers — WAN interfaces, static/floating static routing (CR15)
- WAP — SSID and WPA2-PSK configuration
